apiVersion: apps/v1
kind: Deployment
metadata:
  name: widget-web
spec:
  replicas: 2
  selector:
    matchLabels:
      app: widget-web
  template:
    metadata:
      labels:
        app: widget-web
    spec:
      containers:
        - name: widget-web
          image: registry.prod.amulai.in/widget-web:latest
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /widget.js
              port: 8080
            initialDelaySeconds: 2
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /widget.js
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 32Mi
            limits:
              cpu: 250m
              memory: 64Mi
---
apiVersion: v1
kind: Service
metadata:
  name: widget-web
spec:
  selector:
    app: widget-web
  ports:
    - port: 8080
      targetPort: 8080
